import express from "express";
//import { uploadProfilePic, getProfilePic, deleteProfilePic } from "../controllers/profilePicController.js";
// import { upload } from "../config/cloudinary.js";
// import { protect } from "../middleware/authMiddleware.js";

const router = express.Router();

// router.post("/",   protect, upload.single("profilePic"), uploadProfilePic);
// router.get("/",    protect, getProfilePic);
// router.delete("/", protect, deleteProfilePic);

export default router;